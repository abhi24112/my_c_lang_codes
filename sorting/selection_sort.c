#include <stdio.h>
int main()
{
    int i,j,n,a[50];
    printf("Enter the number of element : ");
    scanf("%d",&n);
    printf("Enter the array : ");
    for(i=0; i<n; i++)
    {
        scanf("%d",&a[i]);
    }
return 0;
}